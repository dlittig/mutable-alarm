import { StyleSheet } from "react-native";

const ActionsStyle = StyleSheet.create({
  touchable: {
    flex: 2,
	justifyContent: 'center',
    alignItems: 'center',
    width: 40,
  }
});

export { ActionsStyle };
